?авай ?азбе?ем кон?еп??ал?н?й ?о??ав, ??нк?ионал и п?име?н?е кон?еп?ии ?еализа?ии дл? cli ??или??. У?или?а б?де? наз?ва???? hypr-appdock-ctl. ?е главна? и един??венна? зада?а ??о о?п?авл??? команд? в ?оке? ?оби?а? и? из а?г?мен?ов в?зова. ?ап?име? зап?о? вида hypr-appdock-ctl layer get ?�json ??или?а п?ев?а?ае? в ???ок? j/layer get; зап?о? hypr-appdock-ctl layer set <данн?е ко?о??е пе?еда???? ?лед???ими а?г?мен?ами> п?ев?а?ае? в ???ок? ?�layer set данн?е ко?о??е пе?еда???? ?лед???ими а?г?мен?ами?? Э?и ???оки должн? пе?едава???? в ?оке? и пол??а?? о?ве? ?е?ве?а. ??ли п?оизо?ла о?ибка код заве??ени? бе????? из о?ве?а ?е?ве?а, е?ли о?ибок не?, ?о код 0

# **?он?еп?и? ??или?? hypr-appdock-ctl**

## **1. ?азна?ение и ??нк?ионал**
У?или?а `hypr-appdock-ctl` ?л?жи? ?ниве??ал?н?м клиен?ом дл? взаимодей??ви? ? демоном ?е?ез IPC-?оке?. ?? кл??ев?е ??нк?ии:

1. **Т?ан?л??и? команд**  
   ??еоб?аз?е? а?г?мен?? CLI в ???ок? дл? ?оке?а:
   ```
   hypr-appdock-ctl <команда> <дей??вие> [данн?е] [--json]
   ??
   j/<команда> <дей??вие> [данн?е]  # ? --json
   <команда> <дей??вие> [данн?е]    # без --json
   ```

2. **?е?еда?а зап?о?ов**  
   ??п?авл?е? ??о?ми?ованн?? ???ок? в Unix-domain ?оке? (`/tmp/hypr-appdock.sock`).

3. **?б?або?ка о?ве?ов**  
   - ??води? ???ой о?ве? ?е?ве?а (?ек??/JSON)
   - ?озв?а?ае? код заве??ени?:
     - `0` п?и ??пе?е
     - ?од о?ибки из о?ве?а (е?ли е???)
     - `1` п?и о?ибка? ?оединени?

---

## **2. Фо?ма? команд**

### **2.1 С???к???а в?зова**
```
hypr-appdock-ctl <command> <action> [data...] [--json]
```

| Ча???      | ?пи?ание                          | ?б?за?ел?но??? |
|------------|-----------------------------------|----------------|
| `command`  | ??новна? команда (нап?име? `layer`) | ?а             |
| `action`   | ?ей??вие (`get`, `set`, `list`)   | ?а             |
| `data`     | ??г?мен?? ?е?ез п?обел            | ?е?            |
| `--json`   | Флаг JSON-?о?ма?а                 | ?е?            |

### **2.2 ??име??**
```bash
# Тек??ов?й зап?о?
hypr-appdock-ctl layer get

# JSON-зап?о?
hypr-appdock-ctl window list --json

# ?оманда ? данн?ми
hypr-appdock-ctl config set theme dark

# ?оманда ? ?о??авн?ми данн?ми
hypr-appdock-ctl notification create "Hello" "Message text"
```

---

## **3. ?б?або?ка о?ве?ов**

### **3.1 У?пе?н?й о?ве?**
- **Тек??ов?й ?ежим**: в?води? данн?е как е???
  ```
  workspace: main
  windows: 3
  ```
- **JSON-?ежим**: в?води? ???ой JSON
  ```json
  {"workspace":"main","windows":3}
  ```
- **?од заве??ени?**: `0`

### **3.2 ??ибка**
- **Тек??ов?й ?ежим**:
  ```
  error: Invalid command [2]
  ```
- **JSON-?ежим**:
  ```json
  {"error":"Invalid command","code":2}
  ```
- **?од заве??ени?**: извлекае??? из о?ве?а (`[2]` или `"code":2`)

### **3.3 ??ибки ?оединени?**
- ??вод: `error: Connection failed`
- ?од: `1`

---

## **4. ?он?еп?и? ?еализа?ии (Go)**

### **4.1 ??новн?е компонен??**
```go
package main

import (
  "fmt"
  "net"
  "os"
  "strings"
)

func main() {
  // 1. ?а??инг а?г?мен?ов
  args, useJSON := parseArgs(os.Args[1:])
  
  // 2. Фо?ми?ование зап?о?а
  request := buildRequest(args, useJSON)
  
  // 3. ??п?авка в ?оке?
  response, err := sendToSocket(request, "/tmp/hypr-appdock.sock")
  if err != nil {
    fmt.Println("error: Connection failed")
    os.Exit(1)
  }
  
  // 4. ?б?або?ка о?ве?а
  exitCode := handleResponse(response, useJSON)
  os.Exit(exitCode)
}
```

### **4.2 ?л??ев?е ??нк?ии**
1. **?а??инг а?г?мен?ов**:
   ```go
   func parseArgs(args []string) (cmdParts []string, useJSON bool) {
     for i, arg := range args {
       if arg == "--json" {
         return append(args[:i], args[i+1:]...), true
       }
     }
     return args, false
   }
   ```

2. **?о???оение зап?о?а**:
   ```go
   func buildRequest(parts []string, jsonFlag bool) string {
     req := strings.Join(parts, " ")
     if jsonFlag {
       return "j/" + req
     }
     return req
   }
   ```

3. **??п?авка в ?оке?**:
   ```go
   func sendToSocket(request, socketPath string) (string, error) {
     conn, err := net.Dial("unix", socketPath)
     if err != nil {
       return "", err
     }
     defer conn.Close()
     
     _, err = conn.Write([]byte(request + "\n"))
     if err != nil {
       return "", err
     }
     
     buf := make([]byte, 4096)
     n, err := conn.Read(buf)
     return string(buf[:n]), err
   }
   ```

4. **?б?або?ка о?ве?а**:
   ```go
   func handleResponse(response string, isJSON bool) int {
     if isJSON {
       var resp struct{ Code int `json:"code"` }
       if json.Unmarshal([]byte(response), &resp) == nil && resp.Code != 0 {
         fmt.Println(response)
         return resp.Code
       }
     } else if strings.HasPrefix(response, "error: ") {
       parts := strings.Split(response, "[")
       if len(parts) > 1 {
         if code, err := strconv.Atoi(strings.TrimSuffix(parts[1], "]")); err == nil {
           fmt.Println(response)
           return code
         }
       }
     }
     
     fmt.Print(response)
     return 0
   }
   ```

---

## **5. ??име?? ?або??**

### **У?пе?н?й зап?о?**
```bash
$ hypr-appdock-ctl workspace get
workspace: main
active: true

$ echo $?
0
```

### **??ибка**
```bash
$ hypr-appdock-ctl invalid cmd
error: Unknown command [2]

$ echo $?
2
```

### **JSON-зап?о?**
```bash
$ hypr-appdock-ctl window list --json
{"windows":["term","browser"]}

$ echo $?
0
```

---

## **6. ??еим??е??ва а??и?ек????**
1. **?инимализм**: делае? ?овно одн? зада?? (пе?еда?а команд)
2. **?ибко???**: подде?живае? л?б?е команд? и данн?е
3. **Совме??имо???**: 
   - ?або?ае? ? ???е??в???ими bash-?к?ип?ами
   - ?одде?живае? JSON дл? ?ложн?? клиен?ов
4. **??оз?а?но???**: в?води? ???ой о?ве? ?е?ве?а без изменений

Такой дизайн ?оо?ве???в?е? Unix-?ило?о?ии "дела?? одн? ве?? и дела?? е? ?о?о?о".
